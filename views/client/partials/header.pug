include ../mixins/search.pug

header.header
    .container
        .row.align-items-center
            .col-3
                .inner-logo
                    a(href="/")
                        img(src="/images/logo.svg", alt="Logo")
            .col-6 
                +search(keyword)

            .col-12
                .inner-menu
                    ul
                        li
                            a(href="/") Trang chủ
